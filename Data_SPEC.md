# 龍門石窟北魏紀年題記資料規格

- 文件版本：0.3-draft
- 文件狀態：供資料設定確認，尚未核准開發
- 建立日期：2026-09-15
- 對應開發規格：`DEVELOPMENT_SPEC.md`
- 來源檔案：`metadataExample/龍門石窟北魏紀年題記_詮釋資料＿正式-re2.xlsx`

## 1. 文件目的

本文件只記錄「龍門石窟北魏紀年題記」分析平台的資料集專屬設定。共用查詢、後分類、版本管理、備份及介面行為由`DEVELOPMENT_SPEC.md`定義。

建立另一個研究資料平台時，應另建一份對應的`Data_SPEC.md`，重新確認來源工作表、唯一鍵、全文欄位、後分類欄位、結果顯示欄位、排序及欄位驗證，不直接沿用本文件的欄名與判定。

## 2. 確認方式

本文件中的設定狀態分為：

- **已確認**：來自使用者已明確核准的需求。
- **類型與功能已確認；驗證待確認**：欄位型態及功能用途已核准，但該欄允許值、空白政策或格式警告仍可調整。
- **建議確認**：依目前Excel內容及已確認功能提出，須由使用者確認或修改。
- **不使用**：來源資料仍保存並顯示於完整資料頁，但不參與該項功能。

欄位矩陣中的「結果顯示」只指每頁10筆的搜尋結果列表。完整資料頁一律顯示該筆紀錄的全部28個原始欄位及原始值。

## 3. 資料集整體設定

| 設定項目 | 設定值 | 狀態 |
|---|---|---|
| 正式資料工作表 | Excel的第一個工作表；工作表名稱不限 | 已確認 |
| 其他工作表 | 全部忽略，不匯入、不核對 | 已確認 |
| 標題列 | 第一個工作表第1列 | 已確認 |
| 資料起始列 | 第2列 | 已確認 |
| 空白尾列 | 忽略所有欄位皆空白的尾列 | 建議確認 |
| 基準資料量 | 153筆 | 來源現況 |
| 基準欄位數 | 28欄 | 來源現況 |
| 唯一鍵 | `序號 (filename)` | 已確認 |
| 全文欄位 | `題記錄文` | 已確認 |
| 多值分隔符號 | 半形分號`;` | 已確認 |
| 多值順序 | 保存原始順序，但不具排序、權重或分析功能 | 已確認 |
| 結果每頁筆數 | 10筆 | 已確認 |
| 主要排序 | `西曆年(year_for_grouping)`遞增 | 已確認 |
| 次要排序 | `序號 (filename)`遞增 | 已確認 |

## 4. 欄位設定矩陣

### 4.1 欄位類型說明

- **識別文字**：看似數字仍以文字保存，不移除前導零。
- **單值文字**：每筆只有一個原始值，以來源字面值分類或顯示。
- **多值文字**：以半形分號拆分供後分類，並保存原始完整字串。
- **整數或文字**：可解析整數另存數值供排序；不可解析內容保留為來源文字，不轉成0。
- **全文文字**：供全文布林查詢及結果全文顯示，不進行斷詞或自動改寫。

### 4.2 逐欄設定

| # | Excel來源欄位 | 欄位類型 | 多值 | 後分類 | 結果顯示 | 排序 | 欄位驗證規則 | 狀態 |
|---:|---|---|:---:|:---:|:---:|---|---|---|
| 1 | `序號 (filename)` | 識別文字 | 否 | 否 | 是：穩定識別碼 | 次要排序 | 不得空白；全表唯一；保持來源字面值及前導零；不限制字數或格式 | 已確認 |
| 2 | `題名 (title)` | 單值文字 | 否 | 否 | 是：結果標題 | 否 | 不得空白；保留原文 | 已確認 |
| 3 | `題記編號(metatags/Epigraph_No)` | 識別文字 | 否 | 否 | 是：題記編號 | 否 | 不得空白；保持來源字面值 | 已確認 |
| 4 | `朝代(time_dynasty)` | 單值文字 | 否 | 否 | 否 | 否 | 接受任意非空來源文字；目前僅`01_北魏` | 類型與功能已確認；驗證待確認 |
| 5 | `年號(time_norm_year)` | 單值文字 | 否 | 是 | 是：主要時間資訊 | 否 | 接受來源文字；不拆解前綴編碼 | 類型與功能已確認；驗證待確認 |
| 6 | `題記時序(metatags/Epigraph_sequence)` | 單值文字 | 否 | 否 | 否 | 否 | 接受來源文字；不自動解析日期 | 類型與功能已確認；驗證待確認 |
| 7 | `中曆月份(metatags/month)` | 單值文字 | 否 | 否 | 是：主要時間資訊 | 否 | 接受`01`至`12`或其他來源文字；不轉換`無`、`缺` | 類型與功能已確認；驗證待確認 |
| 8 | `中曆日(metatags/day)` | 單值文字 | 否 | 否 | 否 | 否 | 接受`01`至`30`或其他來源文字；不作時間處理 | 類型與功能已確認；驗證待確認 |
| 9 | `西曆年(year_for_grouping)` | 整數或文字 | 否 | 是 | 是：主要時間資訊 | 主要排序 | 接受整數或來源文字；文字不得轉成0 | 類型與功能已確認；驗證待確認 |
| 10 | `訖工中曆年月日` | 單值文字 | 否 | 否 | 否 | 否 | 接受來源文字；不自動解析日期 | 類型與功能已確認；驗證待確認 |
| 11 | `石窟群名稱(geo_level1)` | 單值文字 | 否 | 否 | 否 | 否 | 接受任意非空來源文字；目前僅`龍門石窟` | 類型與功能已確認；驗證待確認 |
| 12 | `窟龕名(geo_level2)` | 單值文字 | 否 | 是 | 是：主要位置資訊 | 否 | 接受來源文字；不建立階層驗證 | 類型與功能已確認；驗證待確認 |
| 13 | `窟內位置(geo_level3)` | 單值文字 | 否 | 是 | 是：主要位置資訊 | 否 | 接受來源文字；不建立階層驗證 | 類型與功能已確認；驗證待確認 |
| 14 | `著錄書名(compilation_name)` | 單值文字 | 否 | 是 | 否 | 否 | 接受來源文字；保留書名號及標點 | 類型與功能已確認；驗證待確認 |
| 15 | `原書頁碼(book_code)` | 單值文字 | 否 | 否 | 否 | 否 | 接受頁碼或其他來源文字；不轉換`待補` | 類型與功能已確認；驗證待確認 |
| 16 | `供養人姓名(metagas_authors)` | 多值文字 | 是 | 是 | 否 | 否 | 以半形分號拆分；去除拆分值前後空白；空項目警告 | 類型與功能已確認；驗證待確認 |
| 17 | `供養人性別(doctype)` | 單值文字 | 否 | 是 | 否 | 否 | 接受來源文字；不限定固定選項 | 類型與功能已確認；驗證待確認 |
| 18 | `供養人身份(doc_topic_1)` | 單值文字 | 否 | 是 | 否 | 否 | 接受來源文字；不限定固定選項 | 類型與功能已確認；驗證待確認 |
| 19 | `供養人官職(metatags/donor_title)` | 單值文字 | 否 | 否 | 否 | 否 | 接受來源文字；保留標點 | 類型與功能已確認；驗證待確認 |
| 20 | `造像緣起(doc_topic_2)` | 單值文字 | 否 | 是 | 否 | 否 | 接受來源文字；不限定固定選項 | 類型與功能已確認；驗證待確認 |
| 21 | `主要迴向對象(metatags/main_recipients)` | 多值文字 | 是 | 是 | 否 | 否 | 以半形分號拆分；去除拆分值前後空白；同筆重複值只計一次 | 類型與功能已確認；驗證待確認 |
| 22 | `次要迴向對象(metatags/secondary_recipient)` | 多值文字 | 是 | 是 | 否 | 否 | 以半形分號拆分；去除拆分值前後空白；同筆重複值只計一次 | 類型與功能已確認；驗證待確認 |
| 23 | `受迴向者官職(metatags/recipient_title)` | 單值文字 | 否 | 是 | 否 | 否 | 接受來源文字；分號視為值的一部分，不拆分 | 已確認 |
| 24 | `造像內容(metatags/statues)` | 多值文字 | 是 | 是 | 否 | 否 | 以半形分號拆分；同筆重複值只計一次 | 類型與功能已確認；驗證待確認 |
| 25 | `造像數量` | 整數或文字 | 否 | 是 | 否 | 否 | 可解析正整數另存數值；其他來源文字不轉成0 | 類型與功能已確認；驗證待確認 |
| 26 | `量詞(metatags/quantifier)` | 單值文字 | 否 | 否 | 否 | 否 | 接受來源文字；不限定固定選項 | 類型與功能已確認；驗證待確認 |
| 27 | `主訴願望(metatags/wishes)` | 多值文字 | 是 | 是 | 否 | 否 | 以半形分號拆分；去除拆分值前後空白；同筆重複值只計一次 | 類型與功能已確認；驗證待確認 |
| 28 | `題記錄文` | 全文文字 | 否 | 否 | 是：完整全文 | 否 | 不得空白；保留全部Unicode字元、標點、換行及缺字符號 | 已確認 |

## 5. 後分類設定

### 5.1 已確認啟用順序

以下15個欄位及順序直接決定後分類下拉選單的排列，均已確認：

1. `年號(time_norm_year)`
2. `西曆年(year_for_grouping)`
3. `窟龕名(geo_level2)`
4. `窟內位置(geo_level3)`
5. `著錄書名(compilation_name)`
6. `供養人姓名(metagas_authors)`
7. `供養人性別(doctype)`
8. `供養人身份(doc_topic_1)`
9. `造像緣起(doc_topic_2)`
10. `主要迴向對象(metatags/main_recipients)`
11. `次要迴向對象(metatags/secondary_recipient)`
12. `受迴向者官職(metatags/recipient_title)`
13. `造像內容(metatags/statues)`
14. `造像數量`
15. `主訴願望(metatags/wishes)`

### 5.2 明確不作後分類

- `中曆月份(metatags/month)`
- `中曆日(metatags/day)`
- `供養人官職(metatags/donor_title)`
- `量詞(metatags/quantifier)`
- 唯一鍵、題名、題記編號及全文欄位。
- 其餘未列入5.1的欄位。

## 6. 全文查詢與結果顯示

### 6.1 全文查詢欄位

- 唯一全文查詢欄位：`題記錄文`。
- 不查詢其他Metadata欄位。
- 全文搜尋規則依`DEVELOPMENT_SPEC.md`第8章。

### 6.2 結果列表欄位及順序

以下9個結果列表欄位及顯示順序均已確認：

1. `題名 (title)`：結果標題。
2. `題記編號(metatags/Epigraph_No)`：題記編號。
3. `序號 (filename)`：穩定識別碼。
4. `年號(time_norm_year)`：主要時間資訊。
5. `中曆月份(metatags/month)`：主要時間資訊。
6. `西曆年(year_for_grouping)`：主要時間資訊。
7. `窟龕名(geo_level2)`：主要位置資訊。
8. `窟內位置(geo_level3)`：主要位置資訊。
9. `題記錄文`：完整全文。

### 6.3 全文呈現

- 結果列表使用`題記錄文`的完整欄位值，不使用其他欄位替代。
- 完整資料頁顯示全部28個來源欄位。

## 7. 排序規格

1. 主要排序欄位為`西曆年(year_for_grouping)`。
2. 主要排序方向為遞增。
3. 次要排序欄位為`序號 (filename)`，方向為遞增。

## 8. 資料集匯入驗證設定

1. 本資料集只取Excel第一個工作表，工作表名稱不限。
2. 欄位標題位於第1列，資料由第2列開始。
3. 第1列的28個欄位名稱及順序必須與第4.2節的核准矩陣完全相同。
4. 唯一鍵、必填欄位、欄位類型、單值／多值與個別欄位驗證規則，以第4.2節為準。
5. 全空白尾列是否忽略尚待確認；目前建議設定見第3章及第9章。

阻擋錯誤、警告類型、處理方式及結構變更流程屬於共用系統邏輯，統一依`DEVELOPMENT_SPEC.md`第7章處理，不在本文件重複定義。

## 9. 待使用者確認

1. 空白尾列是否依目前建議忽略。
2. 逐欄矩陣中尚未由使用者明確確認的欄位驗證規則。

## 10. 資料專屬驗收案例

1. 目前來源檔第一個工作表可讀取153筆、28欄。
2. 第二及第三工作表完全不參與匯入、核對或計算。
3. 第一個工作表改名後仍可匯入。
4. `序號 (filename)`的153個值皆非空且唯一，並保持前導零。
5. `題記錄文`是唯一全文查詢欄位。
6. 結果列表每頁10筆，依西曆年及序號規則穩定排序。
7. 結果列表的全文欄位使用完整`題記錄文`。
8. 已核准的多值欄位按半形分號拆分，同筆重複值只計一次。
9. 中曆月份、中曆日、供養人官職及量詞不出現在後分類下拉選單。
10. 後分類值可回查構成結果的全文資料。
