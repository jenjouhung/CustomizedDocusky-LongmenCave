# 實作與驗收狀態

## 0.18 Tag 背景與過濾強調（2026-09-16）

新增獨立 text-view.js 呈現模組：依純文字位置顯示所有 Tag 的淡色背景，符合正式 Tagname＋Term 條件時顯示深藍底白字與外框。搜尋使用底線及高亮；與 Tag 重疊時保留 Tag 背景。列表及完整紀錄共用模組，原始標記全文仍以未著色原文顯示。結果區提供簡短圖例。

Unicode code point 位置先轉換為 JavaScript UTF-16 位移，再依 Tag／搜尋邊界分段；透過文字節點呈現，不執行來源標籤。只使用正式条件，不因草稿或分類控制切換而改變強調。

11項JavaScript測試與Chrome整合驗證通過：全部Tag背景、套用強調、搜尋與過濾重疊、清除還原、跨標記搜尋、詳情與原始值、舊版切換及窄版無水平溢出。資料與匯入格式未改動，未重跑Python匯入測試。public-build已重新建置，未發布。

## 0.17 全文標記與 Tag 後分類（2026-09-16）

沿用現有平台，新增全文標記解析模組、schema 2 靜態資料包與 Metadata／Tag 選單。依 Data_SPEC 已填寫的五個名稱顯示分類；以 Term 聚合並以不重複文件數計算，沒有出現次數統計。既有集合過濾引擎繼續負責兩類分類的 OR／AND 與排除本類條件的計數。

原始28欄仍完整保存與比較，搜尋索引及閱讀高亮使用解析後純文字；完整紀錄可展開安全呈現的原始標記全文。新增 texts/ 衍生資料及解析器版本與 Unicode code point 位置資訊，schema 1 無標記版本仍可讀取。切換版本清除條件與草稿。

變更模組：設定、全文解析、匯入驗證、版本建置、前端資料讀取、分類選單與畫面串接。新增公開介面 parse_text、fields、fieldLabel 與版本資料 text 讀取方法。資料包格式由1升為2；舊格式保持唯讀相容。標記工具與其他未相關的使用者修改未改動。

Python 10項、JavaScript 9項測試通過；Chrome 實測 Tag 顯示名稱、草稿跨類型保留、取消與套用、套用後只見所選項、跨標記搜尋高亮、屬性值不命中、28欄與原始值回查、v0001回讀及390px無水平溢出通過。修正既有版本測試以指定來源檔，避免誤讀詞彙表。

已先備份本機資料，再將 re3 匯入 v0002（153筆）並設為現行版本，保留 v0001。public-build 已重新建置，尚未對外發布。未進行大量資料效能測試或全面視覺重新設計。

## 0.16介面修訂（2026-09-16）

版本訊息移至系統管理選單頂部；移除查詢語法說明入口及後分類區標題；欄位標籤改為「後分類欄位」。公開模式的系統管理選單提供版本訊息及版本閱讀入口，本機管理操作保持隱藏。

變更限於介面HTML、CSS及入口事件綁定，未改變資料包或核心查詢介面。既有Chrome查詢與版面測試通過；另以停用本機管理連線的公開模式檢查版本資訊、選單入口及管理按鈕可見性，全部通過。公開版目錄已重新建置，未對外發布。

## 0.15介面修訂（2026-09-16）

已實作分類值四種排序、每欄排序保留、預設364px可調側欄、左右獨立捲動、全文區上緣筆數與分頁、系統管理收合選單、浮動過濾／取消及套用後只顯示已選值。公開版保留版本閱讀入口。

Chrome整合驗證通過：既有查詢閱讀流程及新版排序、取消、套用後項目隱藏、拖曳分隔線的鍵盤調整、獨立捲動、最後一頁與選單關閉。手機390px版面無水平溢出。

變更模組：介面呈現及後分類排序；新增sortFacets公開函式，資料包格式未改變。匯入、備份、更新工具未修改，未重跑其測試。附件選單細節尚無本輪新增圖片可比對，沿用A版色彩與控制樣式。

## 已提供

- A版緊湊查詢介面；153筆、28欄真實全文資料，v0001。
- 全文布林查詢、連續再查詢、移除條件、全部重設、命中高亮。
- 15欄後分類、同欄OR／跨欄AND、勾選確認與筆數回查。
- 每頁10筆、西曆年排序、全部欄位閱讀。
- 第一工作表匯入、欄位與唯一鍵驗證、獨立版本、比較與切換。
- 本機管理、校驗備份還原、前端更新、靜態公開版建置。
- 沒有後端資料庫；公開版沒有管理服務，未發布到外部網站。

## 已驗證

- Node核心測試7項：查詢語法、跳脫、錯誤位置、累積條件、分類計數、排序、真實全文索引。
- Python版本／备份測試：兩版本匯入、還原指標、備份還原、損壞檔案拒絕。
- Chrome操作：初始153筆與每頁10筆；彌勒查詢39筆；再查詢縮小；錯誤不改條件；分類確認；28欄詳情；零結果；重設。
- 1440及390像素寬度，手機無水平溢出。畫面存於 tests/desktop.png、tests/mobile.png。

## 尚未宣稱通過

- 50,000筆、50 MB與長全文的極限效能及120秒版本比較。
- 不同作業系統：目前提供macOS啟停與原生選檔；未提供Windows啟動器。
- Python管理工具的自動升級；目前更新包僅替換前端。
- 公開主機部署與跨網路下載效能（目前僅產出發布目錄）。

## 模組邊界

資料專案設定集中 tools/config.py；不要在查詢模組新增研究欄位名稱。
query.js負責語法與集合；facets.js負責分類與排序；state.js負責条件；data.js負責檔案讀取與比較；app.js只串接介面。
importer.py不寫檔；packages.py負責版本與交易；manage.py只提供本機管理；updater.py只替換已校驗前端。
修改單一模組先跑 README 模組表指定測試，涉及跨模組再跑瀏覽器整合測試。
