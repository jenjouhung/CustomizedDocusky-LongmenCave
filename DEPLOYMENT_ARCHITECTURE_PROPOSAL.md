# 單機與網路雙模式部署架構

- 文件版本：1.0-confirmed
- 文件狀態：已確認，已納入`SPEC/DEVELOPMENT_SPEC.md`
- 建立日期：2026-09-15

## 1. 已提出的新需求

1. 同一分析平台應可以單機模式運作。
2. 同一分析平台應可以網路版模式運作。
3. 不建立常駐後端資料庫。
4. 兩種模式應共用相同介面與查詢邏輯，避免分別開發兩套系統。

## 2. 建議方案：單一靜態前端＋版本化資料包

建議將平台分為兩個可獨立運作的部分：

1. **分析前端**：由HTML、CSS與JavaScript組成，在Chrome內執行全文查詢、後分類、排序、分頁、版本切換與版本比較。
2. **版本化資料包**：先由資料建置工具讀取Excel，完成驗證、正規化與索引建立，再產生前端可直接讀取的靜態檔案。

建議目錄結構：

```text
platform/
  index.html
  assets/
  data/
    manifest.json
    v0001/
      version.json
      records.json.gz
      facets.json
      search-index.json.gz
      validation.json
    v0002/
      ...
```

`manifest.json`記錄現行版及全部可用版本。每個版本目錄均為可獨立驗證的完整快照，不依賴資料庫。

## 3. 單機模式

- 使用者執行「匯入修訂版」時，本機資料建置工具將Excel轉換成新的靜態版本資料包。
- 建置成功後才更新`manifest.json`的現行版指標；失敗時不改變舊版。
- 一鍵啟動程式只需提供本機靜態檔案服務，不提供查詢API，也不連接資料庫。
- 查詢、後分類與版本比較都在瀏覽器中執行。
- 專案備份是整個`data/`目錄、資料規格與原始Excel的ZIP套件，不再包含資料庫檔案。

## 4. 網路版模式

- 將相同的前端檔與資料包放到任一靜態網站主機，例如學校網頁主機、GitHub Pages、Cloudflare Pages或其他只需提供檔案的服務。
- 網路主機只回傳靜態檔案，不執行查詢邏輯，不需要後端資料庫。
- 更新資料時，管理者先在本機產生並驗證新資料包，再將新版目錄與`manifest.json`發佈到靜態網站。
- 一般網路使用者可查詢、後分類及切換已發佈版本，但不能直接在網站上建立所有人共用的新版本。

## 5. Excel匯入的重要邊界

在「不要後端或可寫入儲存服務」的前提下，網路版無法同時實現以下兩項：

1. 使用者在網頁上傳Excel。
2. 新版資料立即成為所有網路使用者共用的公開版本。

純前端可以在上傳者的瀏覽器中讀取Excel，但產生的資料只存在該瀏覽器或由使用者下載，不會自動更新網路上的共用網站。因此，本建議採「本機匯入與建置＋靜態發布」流程。

## 6. 不建議作為主要方案的作法

### 6.1 只使用IndexedDB

優點是瀏覽器可直接儲存匯入資料，不需要伺服器。但資料限於單一瀏覽器與單一裝置，無法作為網路版共用資料來源，清除瀏覽器資料時也可能遺失。

### 6.2 每次開啟時在瀏覽器直接解析Excel

可減少預處理步驟，但會增加啟動時間、記憶體使用量與瀏覽器相容風險，也不利於建立穩定全文索引及重現舊版。

### 6.3 把資料寫在JavaScript程式碼中

雖然也能靜態發布，但資料與系統邏輯無法分離，與本專案規格相衝突。

## 7. 機密性與網路發布注意事項

- 靜態網站的資料包會下載到使用者瀏覽器。即使介面不顯示某些欄位，具備網站存取權的人仍可取得靜態資料檔。
- 若資料不可公開，必須由學校網站或靜態主機在HTTP層提供存取限制。這不是資料庫，但屬於部署環境的身分驗證功能。
- 不可把前端程式碼當作機密性保護。

## 8. 效能與資料格式建議

- 不建議讓前端一次解析單一巨大JSON。資料應依用途分成紀錄、後分類計數與全文索引檔。
- 靜態主機應啟用Brotli或gzip傳輸壓縮。
- 列表只載入目前頁面所需紀錄；完整全文與完整欄位可分批或按需載入。
- 全文布林運算建議使用預先建立的倒排索引，不在每次查詢時掃描全部全文。
- 資料建置工具應輸出SHA-256清單，供單機與網路版驗證資料包完整性。

## 9. 建議的發布流程

```text
Excel修訂版
  ↓
本機資料建置工具（驗證、建立版本、建立索引）
  ↓
可獨立驗證的靜態資料包
  ├─ 單機模式直接使用
  └─ 上傳至靜態網站作為網路版
```

## 10. 已確認的部署決策

1. 網路版為不需登入或存取限制的公開網站。
2. 網路版的新版資料先在管理者本機建置與驗證，再另行發布。
3. 網路版只提供查詢、後分類、已發布版本切換與完整資料閱讀，不提供共用的線上Excel匯入。
4. 網路版使用者的查詢與後分類條件只存在當次瀏覽器使用期間，不跨裝置保存。

## 11. 模組化實作原則

- 單機與網路版共用同一分析前端、資料包格式、查詢模組與後分類模組。
- 環境差異只能出現在「單機啟動」與「靜態網站發布」邊界，不得分叉成兩套功能程式。
- 版本資料包應有獨立格式規格與驗證器，使前端與Excel匯入工具只依賴該公開契約。
- 每個模組保留簡短說明檔、公開介面和對應測試，後續開發先依影響範圍局部檢查。
- 完整專案掃描與全量回歸測試只在發行前、公開格式變更或跨模組重構時執行，以降低不必要的檢查與Token消耗。
